# CLAUDE.md

Single-page static site: all content lives in `index.html`. Deployed with GitHub
Pages — pushing to `main` publishes the live site immediately.

- This repo is edited from more than one computer. Always `git pull` before editing,
  and commit + push when done.
- Four sibling `Pallet_*` repos under `~/Pallet` share the same page design; design
  changes must be applied to all four together.
- The live site is in active use: do not restructure, rename files, redesign, or
  "clean up" unless explicitly asked.
- This file is publicly served — keep it operational only. Full project context is in
  the private `YanjiD/FW` repo's CLAUDE.md (`~/FW/CLAUDE.md`).
